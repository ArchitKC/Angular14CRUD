export interface userModel{
    id: string,
    email: string,
    first_name: string,
    last_name: string,
    avatar: string
}

export interface productsModel{
    name: string,
    price: Number,
    quantity: Number
}